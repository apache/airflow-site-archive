airflow.providers.keycloak.auth_manager.services.token
======================================================

.. py:module:: airflow.providers.keycloak.auth_manager.services.token


Functions
---------

.. autoapisummary::

   airflow.providers.keycloak.auth_manager.services.token.create_token_for
   airflow.providers.keycloak.auth_manager.services.token.create_client_credentials_token


Module Contents
---------------

.. py:function:: create_token_for(username, password, expiration_time_in_seconds = conf.getint('api_auth', 'jwt_expiration_time'))

.. py:function:: create_client_credentials_token(client_id, client_secret, expiration_time_in_seconds = conf.getint('api_auth', 'jwt_expiration_time'))

   Create token using OAuth2 client_credentials grant type.

   This authentication flow uses the provided client_id and client_secret
   to obtain a token for a service account. The Keycloak client must have:
   - Service accounts roles: ON
   - Client Authentication: ON (confidential client)

   The service account must be configured with the appropriate roles/permissions.


