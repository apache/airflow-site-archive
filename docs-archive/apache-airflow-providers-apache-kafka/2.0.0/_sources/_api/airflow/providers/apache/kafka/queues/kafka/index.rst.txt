airflow.providers.apache.kafka.queues.kafka
===========================================

.. py:module:: airflow.providers.apache.kafka.queues.kafka


Attributes
----------

.. autoapisummary::

   airflow.providers.apache.kafka.queues.kafka.QUEUE_REGEXP


Classes
-------

.. autoapisummary::

   airflow.providers.apache.kafka.queues.kafka.KafkaMessageQueueProvider


Module Contents
---------------

.. py:data:: QUEUE_REGEXP
   :value: '^kafka://'


.. py:class:: KafkaMessageQueueProvider

   Bases: :py:obj:`airflow.providers.common.messaging.providers.base_provider.BaseMessageQueueProvider`


   Configuration for Apache Kafka integration with common-messaging.

   Dispatches ``scheme="kafka"`` to
   :class:`~airflow.providers.apache.kafka.triggers.await_message.AwaitMessageTrigger`,
   which also defines the accepted parameters.


   .. py:attribute:: scheme
      :value: 'kafka'



   .. py:method:: queue_matches(queue)

      Return whether a given queue (string) matches a specific provider's pattern.

      Providers that only support scheme-based dispatch keep this default, which matches
      nothing. Override it to support queue-URI-based dispatch; the implementation must be
      as specific as possible to avoid collision with other providers. Functions in this
      provider should NOT overlap with each other in their matching criteria.

      :param queue: The queue identifier



   .. py:method:: trigger_class()

      Trigger class to use when ``queue_matches`` returns True.



   .. py:method:: trigger_kwargs(queue, **kwargs)

      Parameters passed to the instance of ``trigger_class`` on queue-URI-based dispatch.

      Providers that only support scheme-based dispatch keep this default. Override it
      together with ``queue_matches`` when the trigger needs parameters derived from the
      queue URI.

      :param queue: The queue identifier



