airflow.providers.common.ai.exceptions
======================================

.. py:module:: airflow.providers.common.ai.exceptions


Exceptions
----------

.. autoapisummary::

   airflow.providers.common.ai.exceptions.HITLMaxIterationsError
   airflow.providers.common.ai.exceptions.LLMFileAnalysisError
   airflow.providers.common.ai.exceptions.LLMFileAnalysisUnsupportedFormatError
   airflow.providers.common.ai.exceptions.LLMFileAnalysisLimitExceededError
   airflow.providers.common.ai.exceptions.LLMFileAnalysisMultimodalRequiredError
   airflow.providers.common.ai.exceptions.LowConfidenceError
   airflow.providers.common.ai.exceptions.ManagedAgentInvocationError
   airflow.providers.common.ai.exceptions.LLMBatchInputError
   airflow.providers.common.ai.exceptions.LLMBatchLimitExceededError
   airflow.providers.common.ai.exceptions.LLMBatchModelMismatchError
   airflow.providers.common.ai.exceptions.UnsupportedBatchProviderError
   airflow.providers.common.ai.exceptions.BatchProviderNotYetSupportedError
   airflow.providers.common.ai.exceptions.LLMBatchStaleStateError
   airflow.providers.common.ai.exceptions.LLMBatchOutputTypeError
   airflow.providers.common.ai.exceptions.LLMBatchJobError
   airflow.providers.common.ai.exceptions.LLMBatchTimeoutError
   airflow.providers.common.ai.exceptions.LLMBatchOrphanedIntentError
   airflow.providers.common.ai.exceptions.LLMBatchPartialFailureError
   airflow.providers.common.ai.exceptions.LLMBatchCancelledError
   airflow.providers.common.ai.exceptions.LLMBatchOrphanLookupError
   airflow.providers.common.ai.exceptions.LLMBatchStateReadError


Module Contents
---------------

.. py:exception:: HITLMaxIterationsError

   Bases: :py:obj:`airflow.providers.common.compat.sdk.AirflowException`


   Raised when the HITL review loop exhausts max iterations without approval or rejection.


.. py:exception:: LLMFileAnalysisError

   Bases: :py:obj:`ValueError`


   Base class for file-analysis validation errors.


.. py:exception:: LLMFileAnalysisUnsupportedFormatError

   Bases: :py:obj:`LLMFileAnalysisError`


   Raised when a file format is not supported by LLM file analysis.


.. py:exception:: LLMFileAnalysisLimitExceededError

   Bases: :py:obj:`LLMFileAnalysisError`


   Raised when file-analysis safety limits are exceeded.


.. py:exception:: LLMFileAnalysisMultimodalRequiredError

   Bases: :py:obj:`LLMFileAnalysisUnsupportedFormatError`


   Raised when image/PDF inputs are used without ``multi_modal=True``.


.. py:exception:: LowConfidenceError

   Bases: :py:obj:`ValueError`


   Raised by an LLM operator whose ``DecisionPolicy`` says ``on_uncertain="fail"``.

   The model's confidence in its answer was under the policy's bar, or the model reported no
   confidence at all. Airflow's default retry behaviour retries it like any other exception;
   match it in a retry rule to fail fast instead.


.. py:exception:: ManagedAgentInvocationError

   Bases: :py:obj:`RuntimeError`


   Raised when a managed agent cannot be reached and retrying will not help.

   Reserved for terminal conditions -- bad credentials, a missing agent, a
   revoked quota. Transient failures should propagate unchanged so Airflow's
   task-level retry handles them, and requests the model could fix by
   rephrasing should raise ``pydantic_ai.exceptions.ModelRetry`` instead.


.. py:exception:: LLMBatchInputError

   Bases: :py:obj:`ValueError`


   Base class for ``@task.llm_batch`` errors detected before a batch is submitted.


.. py:exception:: LLMBatchLimitExceededError

   Bases: :py:obj:`LLMBatchInputError`


   Raised when a batch exceeds a provider's request-count or payload-size limit.


.. py:exception:: LLMBatchModelMismatchError

   Bases: :py:obj:`LLMBatchInputError`


   Raised when a per-request ``model`` override resolves to a different adapter than the batch.


.. py:exception:: UnsupportedBatchProviderError

   Bases: :py:obj:`LLMBatchInputError`


   Raised when the connection/model combination has no batch adapter at all.


.. py:exception:: BatchProviderNotYetSupportedError

   Bases: :py:obj:`LLMBatchInputError`


   Raised for a connection type common.ai recognizes but does not yet support for batch.

   Distinct from :class:`UnsupportedBatchProviderError`: that one means "this
   path does not exist"; this one means "this path is known but not built
   yet" (e.g. Azure OpenAI, Bedrock, Vertex).


.. py:exception:: LLMBatchStaleStateError

   Bases: :py:obj:`LLMBatchInputError`


   Raised on retry when the recorded input fingerprint no longer matches and ``on_stale_state="fail"``.


.. py:exception:: LLMBatchOutputTypeError

   Bases: :py:obj:`LLMBatchInputError`


   Raised when ``output_type`` itself cannot produce a JSON Schema (not a per-item validation failure).


.. py:exception:: LLMBatchJobError

   Bases: :py:obj:`RuntimeError`


   Base class for ``@task.llm_batch`` errors detected after a batch is submitted.


.. py:exception:: LLMBatchTimeoutError

   Bases: :py:obj:`LLMBatchJobError`


   Raised when the wall-clock defer budget is exhausted before the batch reaches a terminal state.


.. py:exception:: LLMBatchOrphanedIntentError

   Bases: :py:obj:`LLMBatchJobError`


   Raised when an intent record has no matching batch and ``on_orphaned_intent="fail"``.

   A previous attempt crashed between submitting the batch and recording that it succeeded;
   provider-side recovery (:meth:`~airflow.providers.common.ai.batch.base.BatchAdapter.find_orphaned_batch`)
   found nothing. Unlike a stale-fingerprint mismatch, there is no old batch id to cancel as a
   loss-limiting step here -- resubmitting risks paying twice if the original request actually
   reached the provider and simply could not be recovered.


.. py:exception:: LLMBatchPartialFailureError

   Bases: :py:obj:`LLMBatchJobError`


   Raised when ``fail_on_partial_error=True`` and any request did not produce a valid result.

   Covers provider-side errors, output-validation failures, expired, cancelled and missing
   requests alike; the manifest's ``counts`` says which.


.. py:exception:: LLMBatchCancelledError

   Bases: :py:obj:`LLMBatchJobError`


   Raised when the batch was cancelled (by ``cancel_on_kill``/``cancel_on_timeout`` or out of band).

   Whatever finished before the cancel is fetched and landed first, so the results file is
   complete for the requests that did run. The recorded state is deleted, so the next attempt
   submits a fresh batch instead of re-attaching to the cancelled one.


.. py:exception:: LLMBatchOrphanLookupError

   Bases: :py:obj:`LLMBatchJobError`


   Raised when checking the provider for an orphaned batch failed.

   A previous attempt wrote an intent record but no batch id, and the lookup that would tell
   "the submit never reached the provider" apart from "it did, and is billing" raised. Neither
   answer is known, so the operator refuses to resubmit; the error is retryable.


.. py:exception:: LLMBatchStateReadError

   Bases: :py:obj:`LLMBatchJobError`


   Raised when the run-stable state file exists but cannot be read or parsed.

   Distinct from "no recorded state" (a plain, expected ``None`` -- see
   ``batch/state.py``'s ``read_state``): an I/O failure or a corrupt/malformed
   state file must never be silently treated as "safe to submit a new batch",
   since that is exactly the condition that causes a duplicate, billable
   submission. This is deliberately retryable (a transient object-storage
   blip resolves on its own); a task that fails with this error should be
   retried, not have its state file deleted.


