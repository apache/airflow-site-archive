airflow.providers.edge3.executors.edge_executor
===============================================

.. py:module:: airflow.providers.edge3.executors.edge_executor


Attributes
----------

.. autoapisummary::

   airflow.providers.edge3.executors.edge_executor.CommandType


Classes
-------

.. autoapisummary::

   airflow.providers.edge3.executors.edge_executor.EdgeExecutor


Module Contents
---------------

.. py:data:: CommandType

.. py:class:: EdgeExecutor(*args, **kwargs)

   Bases: :py:obj:`airflow.executors.base_executor.BaseExecutor`


   Implementation of the EdgeExecutor to distribute work to Edge Workers via HTTP.


   .. py:attribute:: supports_multi_team
      :type:  bool
      :value: True



   .. py:attribute:: last_reported_state
      :type:  dict[airflow.models.taskinstancekey.TaskInstanceKey | airflow.models.callback.CallbackKey, airflow.utils.state.TaskInstanceState | str]


   .. py:method:: start(*, session = NEW_SESSION)

      If EdgeExecutor provider is loaded first time, ensure table exists.



   .. py:method:: queue_workload(workload, session)

      Put new workload to queue. Airflow 3 entry point to execute a task.



   .. py:method:: sync(*, session = NEW_SESSION)

      Sync will get called periodically by the heartbeat method.



   .. py:method:: end()

      End the executor.



   .. py:method:: terminate()

      Terminate the executor is not doing anything.



   .. py:method:: revoke_task(*, ti, session = NEW_SESSION)

      Revoke a task instance from the executor.

      This method removes the task from the executor's internal state and deletes
      the corresponding EdgeJobModel record to prevent edge workers from picking it up.

      :param ti: Task instance to revoke
      :param session: Database session



   .. py:method:: try_adopt_task_instances(tis, *, session = NEW_SESSION)

      Adopt the task instances whose job is still in flight in the edge_job table.

      The ``running`` set is empty after a scheduler restart, so the adopted keys go back into it
      to keep slot accounting accurate. Task instances whose job is finished or missing are
      returned so the scheduler clears and re-schedules them.

      :return: any TaskInstances that were unable to be adopted



   .. py:method:: get_cli_commands()
      :staticmethod:


      Vends CLI commands to be included in Airflow CLI.

      Override this method to expose commands via Airflow CLI to manage this executor. This can
      be commands to setup/teardown the executor, inspect state, etc.
      Make sure to choose unique names for those commands, to avoid collisions.



