airflow.providers.duckdb.operators.duckdb
=========================================

.. py:module:: airflow.providers.duckdb.operators.duckdb


Classes
-------

.. autoapisummary::

   airflow.providers.duckdb.operators.duckdb.DuckDBExecuteQueryOperator


Module Contents
---------------

.. py:class:: DuckDBExecuteQueryOperator(*, conn_id = DuckDBHook.default_conn_name, **kwargs)

   Bases: :py:obj:`airflow.providers.common.sql.operators.sql.SQLExecuteQueryOperator`


   Run SQL against an in-process `DuckDB <https://duckdb.org/>`__ database.

   Unlike the other :class:`~airflow.providers.common.sql.operators.sql.SQLExecuteQueryOperator`
   subclasses this operator does not require an Airflow connection to exist: with no
   ``duckdb_default`` connection configured it runs against an in-memory database, which is the common
   case for a task that reads its input, transforms it and writes its output back out.

   .. seealso::
       For more information on how to use this operator, take a look at the guide:
       :ref:`howto/operator:DuckDBExecuteQueryOperator`

   :param sql: the SQL statement, list of statements, or ``.sql`` template file to run.
   :param conn_id: DuckDB :ref:`connection <howto/connection:duckdb>` to use. The default connection
       need not exist, in which case an in-memory database is used. Any other id must exist, and
       raises if it does not.
   :param database: DuckDB database to open, for example ``/tmp/analytics.duckdb``. Overrides both the
       connection and ``hook_params["database"]``.
   :param hook_params: extra keyword arguments for
       :class:`~airflow.providers.duckdb.hooks.duckdb.DuckDBHook`, for example
       ``{"extensions": ["httpfs"], "memory_limit": "2GB"}``.


   .. py:attribute:: ui_color
      :value: '#ffe873'



   .. py:attribute:: hook_class
      :type:  ClassVar[type[airflow.providers.duckdb.hooks.duckdb.DuckDBHook]]


   .. py:method:: get_db_hook()

      Build the hook directly instead of resolving it through the connection.

      ``BaseSQLOperator`` resolves the hook class via ``BaseHook.get_connection(conn_id).get_hook()``,
      which raises when the connection is absent. DuckDB needs no connection to be useful, so the
      hook is constructed here and left to decide what the missing connection means.



