airflow.providers.microsoft.azure.triggers.analysis_services
============================================================

.. py:module:: airflow.providers.microsoft.azure.triggers.analysis_services


Classes
-------

.. autoapisummary::

   airflow.providers.microsoft.azure.triggers.analysis_services.AzureAnalysisServicesRefreshTrigger


Functions
---------

.. autoapisummary::

   airflow.providers.microsoft.azure.triggers.analysis_services.validate_refresh_event
   airflow.providers.microsoft.azure.triggers.analysis_services.validate_completed_refresh_event


Module Contents
---------------

.. py:function:: validate_refresh_event(event)

   Validate a trigger event and return its refresh ID.


.. py:function:: validate_completed_refresh_event(event)

   Validate a terminal trigger event and return the completed refresh ID.


.. py:class:: AzureAnalysisServicesRefreshTrigger(*, conn_id, server_name, database, refresh_id = None, refresh_type = 'full', poke_interval = 60, request_timeout = 60)

   Bases: :py:obj:`airflow.triggers.base.BaseTrigger`


   Poll an Azure Analysis Services model refresh until it reaches a terminal status.

   When ``refresh_id`` is ``None`` the trigger starts a new refresh and yields its ID without
   polling; the caller defers again with that ID to wait for completion. Serializing the actual
   refresh ID is what makes the polling stage survive a triggerer restart.

   :param conn_id: The Azure Analysis Services connection ID.
   :param server_name: The Analysis Services server name.
   :param database: The model database name.
   :param refresh_id: The refresh operation ID to poll, or ``None`` to start a new refresh.
   :param refresh_type: The refresh type used when starting a new refresh.
   :param poke_interval: Time in seconds between status requests.
   :param request_timeout: Timeout in seconds for each HTTP request.


   .. py:attribute:: conn_id


   .. py:attribute:: server_name


   .. py:attribute:: database


   .. py:attribute:: refresh_id
      :value: None



   .. py:attribute:: refresh_type
      :value: 'full'



   .. py:attribute:: poke_interval
      :value: 60



   .. py:attribute:: request_timeout
      :value: 60



   .. py:method:: serialize()

      Serialize the trigger arguments and classpath.



   .. py:method:: run()
      :async:


      Start the refresh when needed, then delegate polling to the hook.



